# 1688 SKU 数据获取

## 1. 目的

供已有 1688 Cookie 读取能力的项目接入测试。目标数据：

- 商品 ID。
- SKUID。
- 每个 SKU 的规格值，例如颜色、尺码。
- 可确认时输出规格名称。
- 商品主图 URL；不将主图当成颜色专属图片。

测试商品：https://detail.1688.com/offer/772946834568.html

此前成功抓包样本商品：1050457853440。两个商品不同，不能将样本字段覆盖情况视为目标商品已验证结果。

## 2. 当前结论

| 项目 | 已知情况 |
|---|---|
| 普通 HTTP 直连 | 对目标商品请求返回 HTTP 200，但内容是约 2355 字节的风控挑战页 |
| 补充浏览器请求头 | 添加 UA、语言、客户端提示、导航请求头后仍返回挑战页 |
| 风控标志 | 响应包含路径片段 /_____tmd_____/punish，以及验证参数 |
| 无 Cookie 的独立浏览器 | 已进行打开测试，尚未确认稳定获取目标商品 SKU |
| 正常商品 HTML | 此前带 Cookie 的抓包样本内存在 SKU 与规格值数组 |
| 携带有效 Cookie 直连 | 应在新项目测试，当前未验证成功，不能保证仅补 Cookie 即可通过 |

**不需要 MTOP 签名，不等于不需要有效会话。HTTP 200 也不等于正常商品页。**

之前将部分样本 SKU 的颜色写成“黑色”没有可靠依据，不应沿用。应以实际解析字段为准，不能根据 ID 顺序推断颜色。

## 3. 推荐接入顺序

### 路径 A：复用有效 Cookie，请求详情页

优先使用已有 Cookie 读取模块，不增加额外登录体系。

1. 从当前有效的 1688 浏览器会话读取 Cookie。
2. 根据目标域名、路径、有效期及安全属性筛选 Cookie；不要把其他站点 Cookie 混入。
3. 向商品详情地址发起 GET 请求，先使用无追踪参数的规范地址。
4. 使用与会话来源浏览器一致的 UA，并发送 HTML 接受类型、中文语言偏好。
5. 如需导航头或来源页，保持与实际请求场景一致，不凭空伪造不匹配的浏览器版本。
6. 使用会话客户端管理响应更新的 Cookie、重定向及压缩内容。
7. 检查响应内容；正常商品页才进入解析。

该路径不调用 MTOP，所以不需要为详情页请求计算 MTOP 签名。浏览器网络环境、会话过期和风控仍可能影响结果。

### 路径 B：在已有浏览器会话中读取

如果 Cookie 直连仍返回验证页：

1. 使用项目现有的浏览器实例或将合法取得的 Cookie 导入专用上下文。
2. 打开商品详情页，允许页面脚本运行。
3. 遇到验证或登录页面，由用户手动处理。
4. 读取主文档响应及当前页面脚本数据。
5. 若页面只异步加载 SKU，则监听页面正常发出的 SKU 查询响应。

使用浏览器时，让浏览器负责 Cookie 域匹配、请求头及会话更新。不要认为把 Cookie 文本放进普通 HTTP 请求就能完整复制浏览器状态。

## 4. HTML 中的 SKU 提取

### 4.1 首选字段

定位详情页内嵌的 [`pieceWeightScaleInfo`](browser_sku/parser.py:21) 数组。

| 原始字段 | 用途 |
|---|---|
| [`skuId`](browser_sku/parser.py:31) | SKU 标识，建议转为字符串存储 |
| [`sku1、sku2 等位置字段`](browser_sku/parser.py:33) | 按数字顺序表示规格值 |

此数据最初用于重量、尺寸等业务，但样本中同时携带 SKUID 与规格值，因此可用于轻量映射。并非所有商品都一定返回该字段。

### 4.2 解析要求

- 解析主文档中的业务数据，不只抓页面可见文字。
- 识别字段后的完整 JSON 值，正确处理嵌套数组、对象及字符串转义。
- 如果外层是 JSON 字符串，先解码外层，再解析内部业务对象。
- 不执行网页脚本字符串来获取对象。
- 不对整份 HTML 反复全局替换反斜杠；这可能破坏真实字符串。
- 仅接受包含 SKU 标识与规格值的记录，按 SKU 标识去重。
- 保留规格位置，不因某一位置为空而把后续属性前移。
- 空数组、字段缺失和格式错误应分别报告。

现有 [`browser_sku/parser.py`](browser_sku/parser.py) 仅为测试原型，包含简化的反转义处理；移植时建议改为完整 JSON 层级解析，不应视为已验证的通用解析器。

### 4.3 属性名称

规格位置字段不保证自带“颜色”“尺码”等名称。

推荐顺序：

1. 从同一商品数据中的规格定义读取名称及顺序。
2. 按位置关联规格值。
3. 找不到名称时只输出位置及原始值，名称留空。

不要无条件认定第一属性是颜色、第二属性是尺码。不要仅根据 S、M、L 等值推断名称。

## 5. MTOP SKU 接口备选

当 HTML 缺少 SKU 数据时，可以分析页面正常调用的接口。

已在此前抓包中定位的端点：

https://h5api.m.1688.com/h5/mtop.1688.wosc.queryofferskuselectormodel/1.0/

请求方式：POST，表单提交业务数据。以下为此前样本的业务参数，不代表所有页面场景都必须使用同一组值：

| 参数 | 样本值 |
|---|---|
| 商品 ID | 1050457853440；目标测试时替换为 772946834568 |
| useCase | 1688detail |
| bizScene | pcod |
| urlParam | actionType=dxOrder&sk=consign |

响应逻辑路径：响应数据 → skuSelectorBizModel → originalSkuInfoMap。

每条 SKU 可包含 SKUID、规格组合文本、规格标识、价格及可下单数量。规格文本中的 HTML 实体应在解析 JSON 后解码；映射属性名称时仍需要规格定义，不宜仅凭分隔符猜测。

### 接入选择

- **优先**监听已有浏览器正常发出的接口响应，让页面负责签名及会话。
- 如直接构造 MTOP 请求，需另行实现并验证时间戳、Token、业务数据与签名的一致性。
- 不能复制旧抓包签名长期使用，也不能认为加 Cookie 就免除了接口签名。
- 不复用示例里的旧价格、库存或规格 ID 作为当前商品结果。

本阶段推荐先完成路径 A；只有确有必要时再增加接口接入复杂度。

## 6. 主图与颜色图

### 商品主图

从当前商品的基础信息、主图列表或明确的主图元数据中提取。

- 必须确认图片属于当前商品，而不是推荐商品、店铺头像或页面装饰。
- 多图时使用明确的封面或主图顺序，不能取整页出现的第一个图片 URL。
- 保留原始 URL；不要直接删除缩略图后缀推断原图。
- 主图未找到时可返回空值，不阻断已成功解析的 SKU。

此前某个商品对比接口确实返回了商品主图，但这不能证明目标商品 HTML 的主图字段路径已确定。HTML 主图定位规则需要在新项目拿到正常页面后核实。

### 颜色图

重量规格数组本身不提供图片。只有拿到明确的“规格值—图片地址”结构，才能关联颜色图。

此前未完整验证详情页及接口里的全部图片映射，因此结论应为“尚未确认”，而不是“确定不存在”。本次范围只要求商品主图。

## 7. 建议输出内容

### 商品层

| 内容 | 规则 |
|---|---|
| 是否成功 | 必须获取到有效 SKU 数据才能视为 SKU 获取成功 |
| 商品 ID | 字符串 |
| 来源 | 标明详情 HTML、浏览器页面或 SKU 接口 |
| 主图 URL | 未找到则为空 |
| SKU 数量 | 去重后的有效记录数 |
| 警告 | 属性名缺失、主图缺失等 |

### SKU 层

| 内容 | 规则 |
|---|---|
| SKUID | 字符串，不使用浮点数 |
| 规格数组 | 每项保留位置、属性名、原始值 |
| 属性名 | 无可靠定义时为空 |
| 原始字段名 | 保留，便于排查映射错误 |

无需输出重量、库存和价格，除非后续业务明确需要。

## 8. 异常处理及日志

| 场景 | 处理 |
|---|---|
| HTTP 200 但内容是挑战页 | 返回访问受限，停止解析 |
| 登录页或会话过期 | 提示更新 Cookie 或进入浏览器确认 |
| 正常详情页但无目标字段 | 返回数据源不适用，考虑浏览器响应监听 |
| JSON 格式异常 | 返回解析失败，保留脱敏诊断 |
| 缺少属性名 | 保留规格位置及值，不猜测 |
| 主图缺失 | 返回 SKU 并给出警告 |
| 网络超时或临时服务错误 | 有限重试，不循环轰炸 |

挑战页识别优先使用明确的挑战地址、脚本结构与页面上下文。不能仅因完整商品页中包含“请登录”或“验证码”等通用文字就判定整个页面被拦截。

日志建议只保留商品 ID、HTTP 状态、响应长度、脱敏后的最终地址、候选字段是否存在和解析数量。不要输出 Cookie、签名、完整风控跳转参数、账户信息或未经脱敏的整页内容。

## 9. 新项目最小验证步骤

- [ ] 确认现有浏览器会话能正常打开目标商品。
- [ ] 使用该会话 Cookie 与一致 UA 发起一次详情页 GET。
- [ ] 确认返回正常商品内容，而不是 HTTP 200 挑战页。
- [ ] 提取 SKUID、规格值及位置。
- [ ] 人工对照商品页核对几个规格组合。
- [ ] 核实属性名及主图在当前页面中的真实路径。
- [ ] 直连失败时改用已有浏览器读取或正常接口响应监听。
- [ ] 分别报告完整成功、部分成功、访问受限及解析失败。

当前无需移植账户配置或牛顿 API 调用代码；复用新项目现有的 Cookie 和浏览器能力即可开始上述验证。

## 10. 本项目独立模块交付进度

本项目首期仅实现路径A，不接入上述浏览器或MTOP备选。使用方法和边界见[独立模块说明](docs/product-sku.md)。第9节是真实商品验证清单，不能用离线测试代替勾选。

- [x] 复用现有Cookie读取，保留有效期、域、路径及安全属性，不强制MTOP字段。
- [x] 新增[独立命令行入口](src/fetch_1688_sku.py)与分模块客户端，不改图搜、API和数据库。
- [x] 实现完整JSON层级解码、商品归属核对、规格空位、无效ID过滤和去重冲突处理。
- [x] 区分访问限制、登录失效、字段缺失、空数组、解析失败及网络失败。
- [x] 提供保守主图元数据规则；规格名为空并告警，未虚构真实路径。
- [x] [27项离线测试](tests/test_product_sku.py)通过，命令行帮助检查通过。
- [ ] 使用有效Cookie实测目标772946834568，核对SKU组合和覆盖范围。
- [ ] 核实目标正常HTML的商品上下文、规格名及主图真实路径。

离线通过不表示在线采集成功；当前返回有效SKU时仍标记部分成功和完整性未知。
